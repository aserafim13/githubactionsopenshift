name: Criar Pod Nginx

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Criar Pod nginx
        run: |
          oc project openshift-operators
          oc run nginx-test --image=nginx --restart=Never --port=80

      - name: Verificar Pod
        run: oc get pods
